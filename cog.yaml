# Configuration for Cog ⚙️
# Reference: https://cog.run/yaml

build:
  gpu: true
  python_version: "3.11"
  python_packages:
    - "diffusers>=0.14.0"
    - "safetensors>=0.2.9"
    - "accelerate>=0.20.0"
    - "requests>=2.28.0"
    - "transformers>=4.42.4"
    - "cog>=0.3.0"

predict: "predict.py:Predictor"
